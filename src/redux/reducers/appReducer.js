import data from './CategoryList.json';

export default () => data;

// import { TEST_ACTION } from '../actions/appActions';

// const testObject = {
//     "TEST_ACTION": (state, action) => { return { ...state, expandUserMenu: action.payload } }
// };

// export default testObject;